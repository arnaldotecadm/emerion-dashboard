# Legacy Business Analysis Skill

Use when mapping legacy reports to dashboard features.

Sources (in order): existing React code, dashboard API contract, product docs, emerion-load-service legacy docs, Delphi source if available.

When examining a legacy report: determine purpose, user, source fields, calculation and filters before proposing a modern dashboard representation.