import PlaceholderPage from "../components/PlaceholderPage";

/** "Configurações" — application/workspace settings (not yet implemented). */
function ConfiguracoesPage() {
  return (
    <PlaceholderPage
      icon="settings"
      title="Configurações"
      description="As preferências do workspace, integrações e permissões de equipe estarão disponíveis aqui em breve."
    />
  );
}

export default ConfiguracoesPage;
